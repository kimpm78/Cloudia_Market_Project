<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudia.backend.common.mapper.CodeMasterMapper">

    <select id="findByCodeTypeAndName" resultType="com.cloudia.backend.common.model.CodeMaster">
        SELECT
            code_type,
            code_type_name,
            code_value,
            code_value_name,
            sort_order
        FROM
            public.code_masters
        WHERE
            code_type = #{codeType} AND code_value = #{codeValue}
    </select>

    <select id="findByCodeType" resultType="com.cloudia.backend.common.model.CodeMaster">
        SELECT
            code_type,
            code_type_name,
            code_value,
            code_value_name,
            sort_order
        FROM
            code_masters
        WHERE
            code_type = #{codeType}
        ORDER BY
            sort_order ASC
    </select>
</mapper>